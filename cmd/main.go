package main

import (
	"github.com/gin-gonic/gin"
	"net/http"
)

//func getTest(c *gin.Context) {
//	c.IndentedJSON(http.StatusOK, "this is the test endpoint.")
//}
//
//const url = "localhost"
//const port = ":42069"

func main() {
	// router := gin.Default()
	// testEndpoint := server.EndpointNoArgs{Route: "/test", Method: getTest}
	// router.GET(testEndpoint.Route, testEndpoint.Method)

	// // This sets the trusted IPs, not really sure how this works
	// router.SetTrustedProxies([]string{url, "::1"})

	// router.Run(url + port)
}
